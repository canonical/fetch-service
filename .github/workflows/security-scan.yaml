name: Security scan
on:
  pull_request:
  push:
    branches:
      - main
      - hotfix/*
      - work/secscan  # For development

jobs:
  go-scans:
    name: Scan Go project
    uses: lengau/starflow/.github/workflows/scan-golang.yaml@work/secscan-golang
